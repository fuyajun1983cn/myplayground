# makefile generated by mcp2make 1.0.2

CC=armcc
LD=armlink
CORE=ARM940T

CCFLAGS = -O1 -g+ -cpu $(CORE)
LDFLAGS = 

OBJS = main.o mpu.o

all: mpu.axf

rebuild: clean all

clean:
	 rm *.o *.axf

mpu.axf: $(OBJS)
	$(LD) $(OBJS) $(LDFLAGS) -o mpu.axf

main.o: main.c regions.h protection.h
	$(CC) -c $(CCFLAGS) main.c -o main.o

mpu.o: mpu.c
	$(CC) -c $(CCFLAGS) mpu.c -o mpu.o

